[automaton]
name = "EvolutionDecision"
initial = "Open"
lifecycle_property = "Status"
states = ["Open", "Approved", "Rejected", "Deferred"]

[[action]]
name = "CreateDecision"
from = []
to = "Open"
kind = "input"
params = ["analysis_id", "decided_by", "rationale", "verification_summary"]
guard = [{ type = "cross_entity_state", entity_type = "Analysis", entity_id_source = "analysis_id", required_status = ["Reviewed"] }]

[[action]]
name = "Approve"
from = ["Open"]
to = "Approved"
kind = "input"

[[action]]
name = "Reject"
from = ["Open"]
to = "Rejected"
kind = "input"

[[action]]
name = "Defer"
from = ["Open"]
to = "Deferred"
kind = "input"

[[invariant]]
name = "ApprovedFollowsOpen"
when = ["Approved"]
assert = "ordering(Open, Approved)"

[[invariant]]
name = "RejectedFollowsOpen"
when = ["Rejected"]
assert = "ordering(Open, Rejected)"
